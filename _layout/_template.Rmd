---
title: {{title}} by {{author}}
---

```{r, echo = FALSE}

htmltools::div(
  htmltools::tags$script(
    type = "text/javascript", 
    src = sprintf(
      "https://cdnjs.cloudflare.com/ajax/libs/iframe-resizer/%s/iframeResizer.min.js",
      "{{ifelse(!is.null(geom$iframeresizer_ver), iframeresizer_ver, '4.2.1')}}"
    )
  ),
  
  htmltools::HTML(
    '<iframe id="gallery-iframe" src="{{embed_url}}" scrolling="no" frameborder="0"',
    'style="min-width: 100%; {{ifelse(!is.null(geom$height), sprintf("height: %s;", geom$height), "")}}" ></iframe>'
  ),
  htmltools::HTML(
    "<script>
     iFrameResize(
       {
         /* Options https://github.com/davidjbradshaw/iframe-resizer#api-documentation */
         // we need to explicitly provide the URL as allowed origin
         // checkOrigin: ['{{embed_url}}']
       },
       '#gallery-iframe'
     );
  </script>"
  )
)
```





